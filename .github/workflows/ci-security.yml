name: CI Security Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # ------------------------------------------------------------------
  # JOB 1 : SAST (Analyse statique du code avec SonarQube)
  # ------------------------------------------------------------------
  sast:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Shallow clones should be disabled for a better branch coverage
      
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v4
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          args: >
            -Dsonar.projectKey=juice-shop-sast
            -Dsonar.ws.timeout=300
      
      - name: SonarQube Quality Gate check
        uses: SonarSource/sonarqube-quality-gate-action@master
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  # ------------------------------------------------------------------
  # JOB 2 : SCA (Analyse des dépendances avec Trivy)
  # ------------------------------------------------------------------
  sca_trivy:
    name: SCA - Trivy Full Scan
    runs-on: self-hosted
    needs: sast
    steps:
      - uses: actions/checkout@v4

      # 1. NETTOYAGE DU CACHE (Indispensable sur self-hosted)
      - name: Clear Trivy Cache
        run: |
          rm -rf ~/.cache/trivy
          rm -rf .trivy

      # 2. SCAN COMPLET (Vuln + Secrets + Config)
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@0.29.0
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          # On écrit le résultat dans un fichier texte
          output: 'trivy_report.txt'
          # On active tous les scanners pour un scan "complet"
          scanners: 'vuln,secret,config'
          # Important : On met exit-code à 0 pour NE PAS bloquer le pipeline
          # (sinon il s'arrêtera dès qu'il trouve la clé RSA ou une faille)
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          # On garde CRITICAL et HIGH pour ne pas avoir un rapport de 1000 pages
          severity: 'CRITICAL,HIGH'
          # On garde la config réseau stable (GHCR)
          db-repository: 'ghcr.io/aquasecurity/trivy-db'
          java-db-repository: 'ghcr.io/aquasecurity/trivy-java-db'

      # 3. AFFICHER LE RESULTAT DANS LES LOGS (Optionnel mais pratique)
      - name: Display Report in Console
        run: cat trivy_report.txt

      # 4. SAUVEGARDER LE RAPPORT (Pour le télécharger)
      - name: Upload Trivy Report
        uses: actions/upload-artifact@v4
        with:
          name: Trivy-Full-Report
          path: trivy_report.txt

  # ------------------------------------------------------------------
  # JOB 3 : DAST (Scan dynamique avec ZAP)
  # ------------------------------------------------------------------
  dast_zap:
    name: DAST - OWASP ZAP
    runs-on: self-hosted
    needs: sca_trivy
    steps:
      - uses: actions/checkout@v4

      # 1. Démarrer l'application cible
      - name: Start Juice Shop
        run: |
          docker run -d --name juice-shop -p 3000:3000 bkimminich/juice-shop
          echo "Waiting for Juice Shop to start..."
          sleep 30

      # 2. Préparer les permissions pour le rapport
      - name: Prepare report directory
        run: |
          chmod 777 ${{ github.workspace }}

      # 3. Lancer le scan ZAP
      # Le flag -I est déjà là, il empêche ZAP de faire échouer le build sur des warnings
      - name: Run ZAP Baseline Scan
        run: |
          docker run --rm --network="host" \
            -v ${{ github.workspace }}:/zap/wrk:rw \
            zaproxy/zap-stable zap-baseline.py \
            -t http://localhost:3000 \
            -r zap_report.html \
            -I 

      # 4. MODIFICATION : Vérification informative (Sans échec)
      - name: Check ZAP Alerts (Informational)
        run: |
          # On vérifie si le rapport contient "Risk: High" juste pour l'info
          if grep -q "Risk: High" ${{ github.workspace }}/zap_report.html; then
            echo "WARNING: Des vulnérabilités HIGH ont été trouvées (Attendu pour Juice Shop)."
            echo "Le pipeline continue car c'est un environnement de démo."
            # J'ai supprimé la ligne 'exit 1' ici pour ne pas bloquer
          else
            echo "Aucune vulnérabilité High détectée."
          fi

      # 5. Upload du rapport
      - name: Upload ZAP Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ZAP-Report
          path: zap_report.html
